{{- $name  := promptStringOnce . "name"  "Your Git display name (e.g. John Doe)" -}}
{{- $email := promptStringOnce . "email" "Your Git email" -}}

[data]
    name  = {{ $name  | quote }}
    email = {{ $email | quote }}
    isWsl = {{ (.chezmoi.kernel.osrelease | lower | contains "microsoft") }}
